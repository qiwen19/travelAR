<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/A400</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavigationTheme" parent="AppTheme">
        <item name="listPreferredItemHeightSmall">90dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="background">@color/sideNavIcon</item>
        <item name="colorControlHighlight">@color/quantum_white_divider</item>
        <item name="android:listDivider">@color/quantum_white_divider</item>
    </style>

    <style name="AccentButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorAccent">@color/white</item>
        <item name="colorButtonNormal">#f00</item>
        <item name="android:disabledAlpha">1.0</item>
    </style>
</resources>